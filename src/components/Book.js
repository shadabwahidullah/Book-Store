import React from "react";

const Book = (props) => {
    return (
        <li>
            <h3>Book name</h3>
            <button type="button"> remove</button>
        </li>
    );
}

export default Book;